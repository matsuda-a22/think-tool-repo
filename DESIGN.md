
# 思考をすっきり整理ツール 設計ドキュメント

## ツールの目的

頭の中で思考がごちゃごちゃするのを可視化してスッキリさせるツール。

---

## 確定した設計

| 項目 | 決定内容 |
|------|---------|
| レイアウト | 横4列（①テーマ → ②書き出し → ③決定・アクション → ④情報ソース） |
| ペイン連動 | ①で小テーマを選択 → ②③④がその内容に切り替わる |
| ②の構造 | 考えごとごとに「テーブル・メモ・ツリーブロック」を必要な数だけ追設（決定ブロックは廃止済み） |
| テーブルブロック | 列数・列名・列タイプ（テキスト/チェックボックス）を自由に設定できる汎用表。行・列の追加・削除・並び替え対応 |
| ③の構造 | 選択中の小テーマに紐づく決定事項・日程・タスク・メモを自由に追加するまとめビュー |
| ③エントリー追加 | 「＋ 追加」ボタン1つ → ドロップダウンで4種類を選択 |
| ③エントリー並び順 | 追加した順（ドラッグで並び替え可） |
| ③エントリー編集 | モーダルで編集、ゴミ箱アイコンで削除 |
| 費用集計 | ③上部に固定。全体合計＋小テーマ合計の2行常時表示、クリックで月次内訳を展開。支払い発生月で集計。**②のブロックは集計対象外** |
| 費用入力形式 | ③のエントリーのみ対象。1エントリーに「年月＋金額」の行を複数追加可能 |
| データ保存 | localStorage 自動保存 ＋ JSON エクスポート |
| AI提案 | 各考えごとの ✨ ボタン → 思考の切り口3件（切り口名＋1〜2文の説明）→ クリックでAIプロンプト付きメモブロックを生成 |
| 画像 | ファイルアップロード（base64）＋ URL プレビュー |
| ①編集 | インライン編集 |
| 関係者タグ | 小テーマ単位で名前を入力、カラーバッジで複数人表示。ペイン②ヘッダーにも表示 |
| 解決フォルダ | ペイン①最下部に1つ設置（全テーマ横断）。小テーマ単位で格納・復元可能、折りたたみ対応。複数テーマ時は「テーマ名 › 小テーマ名」表示 |
| ペイン幅 | ドラッグリサイズ対応（ペイン間の境界を左右にドラッグ） |

---

## ペイン詳細

### ペイン① テーマ
- 大テーマ（複数可）＋ その下に小テーマ（複数可）のツリー構造
- インライン編集（クリックでそのまま入力）
- 小テーマに関係者をカラーバッジで付与（複数可）
- 「✓ 解決」ボタンで解決フォルダに格納
- 最下部に「📁 解決フォルダ」を1つ設置（全テーマ横断）。解決済み小テーマの一覧、復元・削除可。複数テーマ時は「テーマ名 › 小テーマ名」表示

### ペイン② 書き出し
- 選択中の小テーマ名と関係者バッジをヘッダーに表示
- 「考えごと」を自由に追加
- 各考えごとに以下のブロックを必要な数だけ追設：
  - **メモブロック**：自由テキスト入力（自動拡張テキストエリア。AIプロンプト付きの場合は2段構造）
  - **テーブルブロック**：列数・列名・列タイプ（テキスト／チェックボックス）を自由設定できる汎用表
  - **ツリーブロック**：マインドマップ／ロジックツリーで思考を整理
- **費用フィールドなし**（②のブロックは費用集計の対象外）

#### AIプロンプト付きメモブロック

AI提案の切り口をクリックして生成されるメモブロック。通常のメモブロックと同じ `type: "memo"` だが `aiPrompt` フィールドを持つ。

```
┌──────────────────────────────────────┐
│ ✨ 切り口名                           │  ← AIラベル（読み取り専用）
│ なぜその視点が重要かの1〜2文説明        │
├ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┤
│ （自分の考えを書く・自動拡張）          │  ← 編集可能エリア
└──────────────────────────────────────┘
```

データ構造：

```ts
type MemoBlock = {
  id: string
  type: "memo"
  content: string
  aiPrompt?: {
    label: string       // 切り口名（例："コスト構造"）
    description: string // 1〜2文の説明
  }
}
```

#### ツリーブロック仕様

| 項目 | 決定内容 |
|------|---------|
| ブロック種別 | `tree`（マインドマップ・ロジックツリー共通のデータ構造） |
| 表示モード | マインドマップ（中心から放射状）↔ ロジックツリー（左→右の階層）をトグルで切替 |
| 編集方式 | SVG 上のインライン編集（ノードをクリックして直接編集） |
| キーボード操作 | Enter＝同階層に兄弟ノード追加、Tab＝子ノード追加 |
| ルートノード | デフォルトは考えごと名を表示、クリックで上書き編集可 |
| SVGエリア | 固定高さ約360px で表示、ドラッグで移動・ズームボタンで拡大縮小 |
| 更新タイミング | インライン編集に連動してリアルタイムで SVG を再描画 |
| 外部ライブラリ | 使用しない（純粋 SVG で座標計算・描画） |

### ペイン③ 決定・アクション
- 選択中の小テーマに紐づくエントリーを表示するまとめビュー
- **費用集計**（上部固定）
  - 常時表示：全体合計の1行 ＋ 選択中テーマ合計の1行（計2行）
  - クリックで月次内訳を展開
  - 支払い発生月で集計（③のエントリーのみ。②のブロックは含まない）
- **エントリー一覧**（費用集計の下）
  - 「＋ 追加」ボタン → ドロップダウンで以下の4種類を選択
  - 追加した順に並ぶ（ドラッグで並び替え可）
  - 各エントリーはモーダルで編集、ゴミ箱アイコンで削除

#### エントリーの種類

| 種類 | フィールド |
|------|-----------|
| 決定事項 | タイトル ＋ 詳細メモ（任意）＋ 決定済み／未決定トグル ＋ 費用（アコーディオン） |
| 日程 | タイトル ＋ 詳細メモ（任意）＋ 日付（単日 or 期間）＋ 費用（アコーディオン） |
| タスク | チェックボックス ＋ タイトル ＋ 詳細メモ（任意）＋ 期日（単日）＋ 費用（アコーディオン）＊完了時は取り消し線 |
| メモ | タイトル ＋ 詳細メモ（任意）＋ 費用（アコーディオン） |

費用アコーディオン：未入力時は「＋ 費用を追加」ボタンのみ表示。入力済みの場合は折りたたんだ状態でも合計金額をサマリー表示し、クリックで展開。

### ペイン④ 情報ソース
- URL の追加（画像 URL はサムネイル自動プレビュー）
- ローカル画像のアップロード（ドラッグ＆ドロップ対応、base64 保存）
- 各ソースにコメントを追記可能

---

## データ

- **保存先**：`localStorage`（キー：`think-tool-v2`）
- **エクスポート**：JSON ファイル（ツールバーから）
- **インポート**：JSON ファイルの読み込み（ツールバーから）

---

## AI提案機能

### 概要

「何をどう考えるか」まだ整理できていない早い段階に使う。各考えごとの ✨ ボタンを押すと、**思考の切り口**を3件提示する。切り口は「切り口名＋なぜその視点が重要かの1〜2文」の形式。クリックするとAIプロンプト付きメモブロックが生成され、自分の言葉で書き込める状態になる。

AIのテキストをそのままブロックに貼り付けるのではなく、**切り口を提示して自分が考える**ことが目的。

### 設計詳細

| 項目 | 決定内容 |
|------|---------|
| LLMプロバイダー | Google Gemini（`gemini-2.5-flash`）、無料枠で運用 |
| APIキー管理 | アプリ内設定UIで入力 → localStorageに保存（キー名：`think-tool-gemini-key`） |
| トリガー | 各考えごとヘッダー右端の ✨ ＋ シェブロン。クリックでAIパネルが考えごとの直下に展開、もう一度押すと格納 |
| コンテキスト | 小テーマ名 ＋ その考えごと名 ＋ その考えごとのブロック内容（メモ・テーブル・ツリー）。「全考えごとも含める」チェックON時は小テーマ全体を送る |
| 件数 | **3件**（多すぎると選択が面倒になるため） |
| 結果表示 | 考えごとの直下にインライン展開。書き出しエリアの幅は変わらず。AIパネルはmax-height＋内部スクロール |
| 操作 | 切り口をクリック → AIプロンプト付きメモブロックを生成してカーソルを当てる |
| 再取得 | 「再取得」リンクで別の切り口セットを取得（キャッシュクリア） |
| Web検索 | デフォルトOFF。ONにするとGemini Search Groundingで最新情報を元に切り口を出す |
| 深掘り | **廃止**。再取得で別セットをもらう形に |
| 挿入先セレクター | **廃止**。開いた考えごとに自動挿入 |
| コンテキスト対象セレクター | **廃止**。「全考えごとも含める」チェック1つに簡略化 |
| ペイン②グローバルの「AI提案」ボタン | **廃止**。各考えごとの ✨ ボタンに分散 |

### LLMプロンプト出力形式

LLMには以下の形式で3件出力させる：

```
## 切り口名（10字以内）
なぜその視点が重要かの1〜2文説明。

## 切り口名
説明。

## 切り口名
説明。
```

パーサーで `## ` を区切りに `label`（1行目）と `description`（残り）を抽出する。

---

## 未決事項

- ビジュアルテイスト（カラー・フォントの好み）※実使用後に検討
- ツリーブロック：マインドマップ／ロジックツリーの具体的な配色・線スタイル ※実使用後に検討

---

## ファイル

| ファイル | 内容 |
|---------|------|
| `app/` | Next.js ページ・レイアウト |
| `components/` | ペイン・ブロック・AIパネルなどのReactコンポーネント |
| `components/AiPanel.tsx` | AI提案パネル（各考えごとにインライン展開） |
| `components/blocks/MemoBlock.tsx` | メモブロック（AIプロンプト付き2段構造を含む） |
| `lib/types.ts` | 型定義（`MemoBlock.aiPrompt` を含む） |
| `DESIGN.md` | このドキュメント |
